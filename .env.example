# Default: SQLite at backend/data/election_simulator.db (no Postgres required)
# DATABASE_URL=sqlite:///backend/data/election_simulator.db

# Optional: use PostgreSQL instead
# DATABASE_URL=postgresql://localhost/election_simulator
